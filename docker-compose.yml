services:
  taxbot:
    image: ghcr.io/aladin2907/impuesto_bot:latest
    build: .  # fallback for local build
    container_name: taxbot
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
      - TOKENIZERS_PARALLELISM=false
    volumes:
      - hf-cache:/home/app/.cache
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    labels:
      - "com.centurylinklabs.watchtower.enable=true"

  # Auto-update: pulls new images from GHCR and restarts containers
  watchtower:
    image: containrrr/watchtower
    container_name: watchtower
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - WATCHTOWER_CLEANUP=true
      - WATCHTOWER_POLL_INTERVAL=60
      - WATCHTOWER_LABEL_ENABLE=true
      - WATCHTOWER_ROLLING_RESTART=true
    labels:
      - "com.centurylinklabs.watchtower.enable=false"

  # --- Add more bots below ---
  # myotherbot:
  #   image: ghcr.io/aladin2907/other-bot:latest
  #   container_name: myotherbot
  #   restart: unless-stopped
  #   ports:
  #     - "8001:8000"
  #   env_file:
  #     - /home/platon/other-bot/.env
  #   labels:
  #     - "com.centurylinklabs.watchtower.enable=true"

volumes:
  hf-cache:
